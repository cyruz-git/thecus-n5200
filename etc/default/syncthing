# defaults for syncthing
# sourced by /etc/init.d/syncthing

# Change to 0 to disable daemon
ENABLE_DAEMON=1

# Configuration directory
CONFIG_DIR="/etc/syncthing"

# Default options for daemon
OPTIONS="-home=$CONFIG_DIR -logfile=$CONFIG_DIR/syncthing.log"
